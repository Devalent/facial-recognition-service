version: '3.8'

services:
    facial-recognition-application:
        build:
            context: ./app
            dockerfile: Dockerfile
        restart: always
        ports:
            - "3000:3000"
